target/

# IDE metadata generated by the Java language server
.classpath
.project
.settings/

# Local secrets, configs and state never belong in git; deploy/example.* are the templates
.env
*.env
!deploy/example.env
dev.yaml
*.local.yaml
*.db
*.db-shm
*.db-wal

# A jar copied next to the launchers to run them from the checkout
bin/dispatch.jar

# impeccable scratch: decision pages, answer logs, review captures
.impeccable/questions/
.impeccable/review/
.impeccable/decision-*.json
.impeccable/mini-brief-body.md
