# /etc/dispatch/<team>.env - secrets only; chmod 600. Never put these in the YAML file.
TELEGRAM_BOT_TOKEN=
ANTHROPIC_API_KEY=
# Optional: fine-grained GitHub token limited to this team's repositories (read access is enough for M1).
# Without it, git uses the dispatch-<team> user's own credentials.
GH_TOKEN=
